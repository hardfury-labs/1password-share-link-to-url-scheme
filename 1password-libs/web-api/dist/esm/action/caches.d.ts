import { Context } from "./context";
/**
 * clearAllCaches clears all caches.
 */
export declare const clearAllCaches: () => void;
/**
 * clearAllCachesForContext clears all contexts for a given cache.
 *
 * @param c Context to clear caches for
 */
export declare const clearAllCachesForContext: (c: Context) => void;
