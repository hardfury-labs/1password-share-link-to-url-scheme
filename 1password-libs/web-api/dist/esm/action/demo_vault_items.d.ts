import * as model from "../model";
import { Context } from "./context";
export declare const generateDemoVaultItems: (c: Context, numberOfItems: number, vault: model.Vault, templates: model.VaultItemTemplate[]) => Promise<void>;
